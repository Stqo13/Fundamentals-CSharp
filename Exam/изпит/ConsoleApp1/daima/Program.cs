using System;

namespace daima
{
    class Program
    {
        static void Main(string[] args)
        {
            /*
             if (command.StartsWith("Travel"))
            {
                int travelDistance = int.Parse(command.Split(" ")[1]);

                if (fuel >= travelDistance)
                {
                    fuel -= travelDistance;
                    Console.WriteLine($"The spaceship traveled {travelDistance} light-years.");
                }
                else
                {
                    Console.WriteLine("Mission failed.");
                    missionFailed = true;
                    break;
                }
            }
            else if (command.StartsWith("Enemy"))
            {
                int enemyArmor = int.Parse(command.Split(" ")[1]);

                if (ammunition >= enemyArmor)
                {
                    ammunition -= enemyArmor;
                    Console.WriteLine($"An enemy with {enemyArmor} armour is defeated.");
                }
                else
                {
                    int fuelConsumed = enemyArmor * 2;
                    if (fuel >= fuelConsumed)
                    {
                        fuel -= fuelConsumed;
                        Console.WriteLine($"An enemy with {enemyArmor} armour is outmaneuvered.");
                    }
                    else
                    {
                        Console.WriteLine("Mission failed.");
                        missionFailed = true;
                        break;
                    }
                }
            }
            else if (command.StartsWith("Repair"))
            {
                int ammunitionToAdd = int.Parse(command.Split(" ")[1]) * 2;
                int fuelToAdd = int.Parse(command.Split(" ")[2]);

                ammunition += ammunitionToAdd;
                fuel += fuelToAdd;

                Console.WriteLine($"Ammunitions added: {ammunitionToAdd}.");
                Console.WriteLine($"Fuel added: {fuelToAdd}.");
            }

            input = Console.ReadLine().Split("||");
            command = input[0];
             */
        }
    }
}
